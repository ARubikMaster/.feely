# .feely image file format

Make the feely executable runnable:
```chmod +x feely```

To convert a .png image to .feely:

```./feely convert /path/to/image.png name_of_new_image```

Download the file named feely if you are on linux. If you are on windows/mac compile the program yourself :)